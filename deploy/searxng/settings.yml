use_default_settings: true

general:
  debug: false
  instance_name: "WebSearch SearXNG"

search:
  safe_search: 0
  autocomplete: ""
  default_lang: "en"
  formats:
    - html
    - json

server:
  secret_key: "websearch-dev-secret-change-in-production"
  limiter: false
  image_proxy: false
